#include <stdio.h>
#include <string.h>

int main(){
	
	char word[105];
	int length;
	scanf("%s", &word);
	length = strlen(word);
	
	for(int i=0; i<length; i++){
		if(){
			printf("YES");
		}
		else{
			printf("NO");
		}
	}
	
	return 0;
}
